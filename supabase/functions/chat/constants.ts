// Model Config
export const GPT_MODEL = "gpt-4o-mini"; //"gpt-3.5-turbo" / "gpt-4-turbo" / "gpt-4" / "gpt-4o-mini"
export const GPT_MODEL_BEHAVIOUR = "You are a helpful assistant that chats naturally with users";

export const MAX_CHAT_HISTORY_CONTEXT = 10; // Controls the chat context sent to the model with every request
